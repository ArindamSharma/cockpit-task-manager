pkgname=cockpit-task-manager
pkgver=VERSION
pkgrel=1
pkgdesc='Cockpit Task Manager Module'
arch=('x86_64')
url='https://github.com/ArindamSharma/task-manager'
license=(LGPL)
source=("SOURCE")
sha256sums=('SKIP')

package() {
  depends=(cockpit)
  cd $pkgname
  make DESTDIR="$pkgdir" install PREFIX=/usr
}
